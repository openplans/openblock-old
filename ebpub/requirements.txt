# Editable Django checkout, so we can patch it.
-e svn+http://code.djangoproject.com/svn/django/tags/releases/1.2.3/#egg=django==1.2.3

# Not specifying GDAL version, since it depends on our platform. Rely on obadmin/pavement.py to work around this.
GDAL

django-static==1.4.0
psycopg2>=2.2
pyyaml>=3
slimmer>=0.1.30
